#include <stdio.h>

int main(void) {
    // Begrüßung
    printf("Hello from C!\n");

    // Ganzzahl, Gleitkommazahl, String
    printf("Integer: %d, Float: %.2f, String: %s\n", 42, 3.14, "test");

    // Ausgabe mit puts (setzt automatisch einen Zeilenumbruch)
    puts("This is puts output");

    return 0;
}
